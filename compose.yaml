services:
  db:
    image: postgres:18
    container_name: items
    environment:
      POSTGRES_PASSWORD: cookies
      POSTGRES_USER: alkan
      POSTGRES_DB: PostgreDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: