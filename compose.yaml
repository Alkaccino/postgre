services:
  db:
    image: postgres:18
    container_name: items
    environment:
      POSTGRE_PASSWORD: 1234
      POSTGRE_USER: alkan
      POSTGRES_DB: PostgreDB
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: